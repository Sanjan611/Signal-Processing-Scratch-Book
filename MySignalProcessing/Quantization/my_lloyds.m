function [partition, codebook, distortion, rel_distortion] = my_lloyds(training_set, ini_codebook)
    % this is an optimisation algorithm. training_set should be typical
    % data for the message to be quantised. ini_codebook is the initial
    % guess of the codebook values. Optimisation terminated if relative
    % distortion is less than 10^(-7).
    
    
    
end